namespace MediaScrapper.Authentication;

public static class Auth0AuthenticationExtensionAuthorizationPolicy
{
    public const string Name = nameof(Auth0AuthenticationExtensionAuthorizationPolicy);
    public const string RelatedAuthenticationScheme = $"{Name}AuthenticationScheme";
}
